# Configuración del Servidor
PORT=3000
NODE_ENV=development

# Configuración de la Base de Datos
DB_HOST=localhost
DB_PORT=3306
DB_USER=tu_usuario_mysql
DB_PASSWORD=tu_contraseña_mysql
DB_NAME=rifas

# Configuración de Sesiones (CAMBIAR POR UNA CLAVE SEGURA)
SESSION_SECRET=tu_clave_secreta_muy_segura_aqui_cambiala_por_una_real

# Notas:
# 1. Copia este archivo a .env
# 2. Cambia todos los valores por los reales de tu entorno
# 3. NUNCA subas el archivo .env real a GitHub
