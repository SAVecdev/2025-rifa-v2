<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/output.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-slate-900 text-slate-100 min-h-screen">
    <!-- Layout para administración -->
    <div class="flex min-h-screen">
        <!-- Sidebar de administración -->
        <aside class="w-64 bg-slate-800 shadow-lg">
            <div class="p-6">
                <h2 class="text-xl font-bold text-blue-400 mb-6">Panel Admin</h2>
                <nav class="space-y-2">
                    <a href="/admin" class="flex items-center px-4 py-2 text-slate-300 hover:bg-slate-700 hover:text-white rounded-lg transition-colors">
                        <i class="fas fa-tachometer-alt mr-3"></i>Dashboard
                    </a>
                    <a href="/admin/rifas" class="flex items-center px-4 py-2 text-slate-300 hover:bg-slate-700 hover:text-white rounded-lg transition-colors">
                        <i class="fas fa-ticket-alt mr-3"></i>Rifas
                    </a>
                    <a href="/admin/users" class="flex items-center px-4 py-2 text-slate-300 hover:bg-slate-700 hover:text-white rounded-lg transition-colors">
                        <i class="fas fa-users mr-3"></i>Usuarios
                    </a>
                    <a href="/admin/reports" class="flex items-center px-4 py-2 text-slate-300 hover:bg-slate-700 hover:text-white rounded-lg transition-colors">
                        <i class="fas fa-chart-bar mr-3"></i>Reportes
                    </a>
                    <a href="/" class="flex items-center px-4 py-2 text-slate-300 hover:bg-slate-700 hover:text-white rounded-lg transition-colors">
                        <i class="fas fa-home mr-3"></i>Ir al sitio
                    </a>
                </nav>
            </div>
        </aside>
        
        <!-- Contenido principal -->
        <div class="flex-1 flex flex-col">
            <!-- Header del admin -->
            <header class="bg-slate-800 shadow-sm p-4">
                <div class="flex items-center justify-between">
                    <h1 class="text-xl font-semibold text-white"><%= title %></h1>
                    <div class="flex items-center space-x-4">
                        <span class="text-slate-300">Admin</span>
                        <button class="text-slate-400 hover:text-white">
                            <i class="fas fa-sign-out-alt"></i>
                        </button>
                    </div>
                </div>
            </header>
            
            <!-- Contenido -->
            <main class="flex-1 p-6 bg-slate-900">
                <%- body %>
            </main>
        </div>
    </div>
    
    <!-- Scripts -->
    <script src="/js/main.js"></script>
</body>
</html>
